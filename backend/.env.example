# MariaDB remains the durable source of truth.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=watch_store

# Start Redis with: docker compose up -d redis
REDIS_URL=redis://127.0.0.1:6379

PORT=4000
PASSWORD_SALT=watch-store-salt